import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "io.ionic.starter",
  appName: "ionic_vue_quickstart",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
